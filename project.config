[access]
	inheritFrom = Public-Projects
[submit]
	action = inherit
[access "refs/*"]
	owner = group aws-gerrit
	create = group aws-gerrit
	forgeAuthor = group aws-gerrit
	forgeCommitter = group aws-gerrit
	push = group aws-gerrit
